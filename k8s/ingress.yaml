apiVersion: v1
kind: Service
metadata:
  name: geraeteausleihe
  namespace: geraeteausleihe
spec:
  ingressClassName: traefik
  rules:
    - host: geraeteausleihe.CHANGE_ME
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: geraeteausleihe
                port:
                  number: 80
